<?php

// @phpcs:disable Generic.Commenting.DocComment.MissingShort

/**
 * Test file for DisallowTypeLongNamesSniff - Generic Types.
 *
 * Tests detection of long type names in generic type syntax.
 */

// Generic types with long forms
/**
 * @param array<bool> $bool Array Should be array<bool>
 * @param array<string, int> $map Should be array<string, int>
 * @param Collection<bool, int> $collection Should be Collection<bool, int>
 *
 * @return Map<int, bool> Should be Map<int, bool>
 */
function testGenericTypes($bool, $map, $collection)
{
    return [];
}

/**
 * More complex generic examples.
 *
 * @param Generator<int, bool> $generator Should be Generator<int, bool>
 * @param Promise<bool> $promise Should be Promise<bool>
 * @param Traversable<string, int> $traversable Should be Traversable<string, int>
 *
 * @return Iterator<int, bool> Should be Iterator<int, bool>
 */
function testComplexGenerics($generator, $promise, $traversable)
{
    return [];
}